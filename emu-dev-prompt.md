# emu 开发偏好

这份文档记录 emu 在 GenericAgent 项目上的二次开发习惯。后续让 AI 参与开发时，优先遵守这里的约定。

## 基本原则

- 先读代码，再改代码。不要基于猜测直接改。
- 代码改动要小而明确，围绕当前目标处理，不做无关重构。
- 如果上游 `main` 有功能更新，合并时优先保留上游功能，再叠加本地可读性和行为改动。
- 只在有必要时修改文档、模板、配置向导。个人开发偏好类调整默认只改实际运行代码。
- 不要随意丢弃已有改动；遇到冲突时要说明保留哪边、为什么。

## 代码风格

- 避免多条语句挤在一行，尤其是 `if ...: ...`、`try: ...`、循环和回调逻辑。
- 复杂逻辑要拆开写，让代码阅读能力一般的人也能顺着读懂。
- 可以加注释，但注释要解释运行路径、状态变量、协议含义或容易误解的地方。
- 不要加空泛注释，例如“设置变量”“调用函数”这类没有信息量的注释。
- 格式化以提升可读性为目标，不为了统一风格做大范围无关改动。

## Git 和合并

- 合并 `main` 到开发分支时，目标是同时保留上游功能改动和本地可读性改动。
- 如果合并出现冲突，先比较每个冲突点的两边内容，整理差异和影响，再告诉 emu，由 emu 决定具体怎么合并。
- 未得到明确决定前，不要擅自选择一边覆盖另一边。
- 解决冲突后要检查：
  - 没有冲突标记
  - `python3 -m py_compile` 至少覆盖改动过的 Python 文件
  - `git diff --check` 没有明显空白错误

## Commit 信息风格

- commit message 用中文。
- commit 信息正文要覆盖主要改动点，不要漏掉关键行为变化。
- 标题建议使用 `fix(scope): 中文说明`、`refactor(scope): 中文说明`、`chore(scope): 中文说明` 这类格式。
- scope 尽量写具体模块，例如 `wechat`、`agent`、`llm`、`deps`、`docs`。
- 标题只写一句话，描述结果，不写过程。
- 如果改动不止一个点，正文用列表写清楚，每个主要改动都要体现。
- 不要用过宽泛的标题，例如“修改代码”“修复问题”“优化逻辑”。
- 如果只是让我写提交信息，不要自动执行 `git add` 或 `git commit`。

## 配置和个人改动

- 个人开发偏好的运行时调整，只改实际代码，不同步改 README、模板、配置向导，除非明确要求。
- 私有配置文件如 `mykey.py` 不应随意修改或纳入提交。
- 如果文件已经被 Git 跟踪，仅加入 `.gitignore` 不会停止跟踪；需要明确说明是否要 `git rm --cached`。

## 交流方式

- 先给结论，再解释关键原因。
- 复杂流程要按运行路径讲，不要一开始陷入细节。
- 用户问“是不是”时，直接判断，并指出容易混淆的点。
- 修改前说明要改哪里，修改后说明验证过什么。
